/*
package dk.dtu.mitprojektarkiv;

import android.graphics.Canvas;
import android.graphics.Color;
import android.graphics.Paint;
import android.graphics.RectF;


public class ImageDraw {

    // The positions of the ball
    float xPosition;
    float yPosition;
    int boundWidth;
    int boundHeigth;
    /*
    int posZ; // For further use later
    */
  /*
    private RectF ballBounds;

    // Creating a Constructor for the ball to be passed on
    public ImageDraw(float posX, float yPosition, int boundWidth, int boundHeigth) {
        this.xPosition = posX;
        this.yPosition = yPosition;
        this.boundWidth = boundWidth;
        this.boundHeigth = boundHeigth;
        this.ballBounds = new RectF();

    }

    // Draw Method for the ball with bounds
    public void drawBall(Canvas canvas, Paint paint) {

        ballBounds.set(this.xPosition, this.yPosition, this.boundWidth + this.xPosition, this.boundHeigth + this.yPosition);
        paint.setColor(Color.BLACK);
        canvas.drawOval(this.ballBounds, paint);
    }

    // Draw Method for a sqare with bounds
    public void drawSquare(Canvas canvas, Paint paint) {

        ballBounds.set(this.xPosition, this.yPosition, this.boundWidth + this.xPosition, this.boundHeigth + this.yPosition);
        paint.setColor(Color.BLACK);
        canvas.drawRect(this.ballBounds, paint);
    }




}

*/